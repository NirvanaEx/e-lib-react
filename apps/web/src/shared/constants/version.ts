/**
 * Версия приложения — единственное место, где её задаём (номер в package.json
 * пакетов держим таким же).
 *
 * Схема: 2.<функциональный блок>.<правки внутри блока>.
 * Двойка — второе поколение интерфейса: после 1.x приложение переросло из
 * январской версии (доступы, версии файлов, плиточный вид) в текущее.
 * Minor считаем по функциональным блокам, выпущенным после 1.x:
 *   1) библиотека и пользовательский блок, hero, плиточный вид;
 *   2) мобильная адаптивность и расширенный поиск;
 *   3) поддержка, фоны и переработанный вход;
 *   4) настройки на вкладках, язык, 30-дневная сессия;
 *   5) аватарки и тема «жидкое стекло»;
 *   6) оформление сайта в админке: карусель шапки, лого, кадрирование;
 *   7) защита от перебора паролей, статистика активности, плоский стиль;
 *   8) справочник должностей, глобальные категории, дерево подотделов;
 *   9) production-обвязка и компактные таблицы: № в списках, дерево отделов,
 *      диапазоны статистики, переработанный расширенный поиск.
 * Patch — накопительные правки поверх текущего блока.
 */
export const APP_VERSION = "2.9.0";
