# Current Stage

- Default login route now lands on the user panel (shared library).
- Shared library lists all files with lock icons for inaccessible items and no download action.
- "My files" renamed to "My uploaded files" and shows lock status based on access.
- Sidebar footer (library switch/storage) pinned to the bottom with a dedicated scroll area for the menu list.
- Uploader is always preserved in file access on approval, and access lists persist across access type changes.
- Auto-refresh now skips `/auth/me` while a temp-password change is required to avoid clearing auth.
- Lock icon now appears when a file is not downloadable either due to access or missing assets; download button is hidden in that case.
- Favorites added in My Library with a dedicated page, API, and migration.
- Requests/My uploaded files sidebar items are shown only when file submission is allowed.
- Lock icon size reduced and aligned in shared/my library tables.
- Trash now lists files, versions, and assets with type-specific restore and permanent delete actions.
- Sidebar scrollbar now aligns with the drawer edge (scroll area expanded, content padding preserved).
- listTrash language handling fixed to avoid TS compile errors.
- Lock icon column now centered with reduced padding in user library tables.
- Lock icon column shifted slightly right for horizontal alignment.
- Versions now require an initial file on creation, and version assets are immutable in the UI; deleting/restoring a version soft-deletes/restores its assets.
- Version creation supports multiple language files (one file per language) in the create form.
- Dashboard requests now support search, column sorting, row-click details with asset downloads, and icon actions; user requests mirror tabs/search/sorting.
- Favorites toggles added to user library tables, with access shown as icons and wider title/category columns.
- Favorite add/remove now shows toast notifications.
- Favorites matching now normalizes IDs when building `isFavorite` in user list responses.
- Shared library favorites now stay highlighted by merging favorite IDs client-side; file tables give more room to title/category by shrinking static columns.
- Submit request form now places the admin comment at the end with distinct styling and uses an icon button for adding extra language files.
- Dashboard files and requests tables now use access icons and wider title/category columns, with tighter widths for static fields; my-library request table follows the same spacing.
- Navbar now prefers a shortened surname + initials display when available; publication request details and history highlight user/admin comments, and user request rows open a details dialog with comments.
- Comment columns removed from request tables; comments remain available in request details dialogs.
- File-related tables now allow wrapping in title/category cells to avoid horizontal scrolling.
- Version restore is now only available via Trash; the file details view no longer shows a restore action for deleted versions.
- `/auth/me` now returns full user details (surname/name/patronymic), so the navbar can render the short "Фамилия И.О." label after refresh.
- User labels now show as `login (Фамилия И.О.)` across admin-facing filters/tables and access lists; wrapped columns now use fixed layout with word wrapping and clipping for long text.
- File-related tables now allocate widths to fixed columns and wrap primary text columns to avoid horizontal scroll bars.
- DataTable headers now wrap instead of truncating, and file-related tables rely on flexible column sizing with wrapping to fit the screen without horizontal scroll.
- DataTable UI reverted to the latest commit state; file-related tables no longer use wrap/layout overrides.
- Trash list now hides assets for deleted versions and shows file title/details in the trash table.
- My Library sidebar now includes a Department files tab with a dedicated endpoint and list view filtered to the user's department scope.
- Fixed listUserFiles filter naming to avoid compile errors when the department files view is enabled.
- Department files now support update requests via a leading update icon, with new/update request type icons shown in user/admin request tables.
- Update requests create new versions on approval, and admin request details include version history for updates.
- My uploaded files now flag updates by other users with a warning icon and a details dialog showing who updated.
- Fixed Russian labels added for request types/department files by using Unicode escapes to avoid encoding issues.
- DataTable now supports fixed layout and per-column wrapping; My Library file tables use fixed column widths with wrapping for long titles/headers.
- Centralized My Library file table column sizing/word-wrap in `fileTableLayout.ts` for easy manual tuning.
- Shared library file table now uses its own layout config; user library tables have per-view layouts.
- Added a single leading status column (no header) that groups favorites/lock/update/warning icons.
- Fixed widths (px) for access, languages, file size, created/updated date, and download columns.
- DataTable now allows per-table container overflow control; file tables use page-level horizontal overflow instead of inner table scrollbars.
- Added per-version title snapshots and history display in dashboard file/request details.
- Users can view/download archived versions when enabled per file; admin access tab includes a toggle (default on).
- Metadata updates now backfill the current version’s archived title/description if missing, so history retains old values.
- Setting a previous version as current now restores its archived title/description when available.
- User file details now show archived versions at the bottom in a collapsed accordion by default.
- My Library file rows now open a details dialog with archived versions collapsed at the bottom; the warning icon opens the "updated by other" info.
- Version cards now show who created the version in user/admin details.
- My uploaded files now show the user's own latest version data (title/assets/dates), with a deleted/missing status indicator and downloads tied to that version.
- Dashboard file row details now include archived versions, creator/updater info, and access shown as icons in a separate block.
- Detail views now show created/updated by across user/admin file dialogs, and language/asset lists show all available file languages.
- File detail views now only show the creator (no updater) in the details block.
- Shared library now supports table and card views with a toggle in the filters bar.
- Fixed shared library card view to use the section label helper.
- Refined shared library card layout with a tighter grid, clearer hierarchy, and improved visual styling.
- Compact card sizing and removed shadow effects for the shared library cards.
- Shared library cards now use a columnar label/value layout with fixed card widths that scale by screen size.
- Shared library cards now use single-line aligned values (ellipsis for long titles/paths) with a more compact layout.
- Access and download icons are now left-aligned and sized consistently in shared library cards.
- Fixed access icon alignment to start in shared library cards.
- Removed the created-at column from user/shared library file tables and set non-wrapping header min widths for access, languages, file size, updated date, and download columns.
- Dashboard requests now allow wrapping in title/department/category cells; dashboard files table adds a title column, removes created-at, and shows created-at inside the file details dialog.
- Shared/My library tables now clip hover highlights to the rounded container without forcing the table to expand beyond the page width.
- Added department_closed access type handling: UI labels/icons updated, access lists now show departments for non-public types, and access checks only honor user lists for restricted.
- Shared library status column widened with tighter padding so favorite/lock icons no longer overlap.

- Security: throttler TTL fixed (v5 counts milliseconds, so `ttl: 60` was a 60 ms window and left login brute force unthrottled); global bucket raised to 300/min, login 10/min.
- Security: per-account and per-IP login lockout backed by a new `login_attempts` table (`LOGIN_MAX_ATTEMPTS`, `LOGIN_LOCKOUT_MINUTES`), with a neutral 429 that does not reveal whether the login exists.
- Security: password policy centralised in `IsPassword()` (8+ chars, letter + digit, 128 max) and reusing the current password is rejected; temp passwords widened to 12 chars + digit.
- Security: on-disk asset paths removed from version payloads; per-user access lists returned only to holders of `file.access.update` (others get `accessUsersCount`).
- Security: uploads land under a fixed `.tmp` name until the extension allowlist accepts them; `TRUST_PROXY` gates X-Forwarded-For (off by default); missing `@Access` added to user request endpoints.
- Statistics: file views are now recorded (`file_views`, 15-minute dedupe per user/file), downloads and views both store IP and user agent.
- Statistics: new endpoints — overview with previous-period deltas, activity feed (who/when/what, filters + CSV export), top files, top users, by department, by period, per-file activity, login activity, extended storage (trash size, disk free, breakdown by extension, largest files).
- Statistics page rebuilt with tabs (overview / activity / files / users / storage / security), area chart of downloads vs views, filterable activity log and CSV export.
- UI: shared `SectionCard` replaces hand-rolled panels; `StatCard` no longer forces a white gradient in dark mode and supports icon/hint/trend; content, seed and appearance pages aligned to the same surface.
- UI: sidebar storage widget is now an interactive card — progress against quota or real disk space, loading skeleton, collapsed tooltip, and it opens the storage tab of the statistics page.

- UI: storage widget removed from the sidebar; the numbers live on the Storage tab of the statistics page.
- UI: flat surfaces — `--shadow: none` in every theme variant, the `GLASS_SHEEN` gloss dropped from Paper, button elevation off, and the coloured glows / hover lifts under cards replaced by a border + light tint.
- UI: `success`/`warning`/`error`/`info` added to the palette; without them MUI fell back to its stock burnt orange (#ed6c02). Pending counters now use the theme colour in both the tab and the sidebar, and "deleted" chips are neutral outlines.
- UI: the "Наполнение данных" (seed) page is gone from the dashboard — menu entry, route and page removed. `/api/dashboard/seed` still exists behind `section.add` for bootstrapping a fresh install.

- Fix: deleting a section counted only live files, so a section whose documents were all in the trash deleted fine and left them orphaned (`file_items.section_id` is ON DELETE SET NULL) — restoring one gave back a broken file. Trashed files now block the delete too, and both sections and categories answer with a structured code plus counts that the UI turns into "what exactly is in the way".
- Icon library grown from 166 to 355 icons, every entry tagged with Russian keywords so the picker can be searched in the interface language.
- New access type `restricted_open`: a restricted file with the "Доступ всем" switch on — it keeps its department attachments but everyone may read and download it, and the per-user list is hidden and not saved. The switch sits under the "Ограниченный" option (it was first attached to "Закрытый для отдела" by mistake; migration 20260729120000 renames any `department_open` rows). "Закрытый для отдела" stays out of the shared library for everyone, including its own department, and out of the section counters — it is only reachable from the department tab. Access labels/colours moved into `shared/utils/accessType.ts` — they used to be copy-pasted across six pages.

## Next

- Run the new migration for rejection reasons if not done yet.
- Run the new migration for soft-deleting version assets if not done yet.
- Verify shared library shows locked items correctly and download is disabled.
- Confirm creator access persists after toggling public/restricted in file access settings.
- Verify shared library favorites stay highlighted after refresh.
- Verify the Department files tab loads for users with a department and shows department-scoped files.
- Run the new request type migration and verify update requests flow from Department files into Requests.
- Run `npm run migrate` for `20260729000000_activity_tracking` (file_views, downloads.ip/user_agent, login_attempts).
- Optionally set `STORAGE_QUOTA_GB` so the sidebar widget fills against a quota instead of free disk space.
- Set `TRUST_PROXY` only if a reverse proxy is put in front of the API; leaving it off keeps X-Forwarded-For unspoofable.
